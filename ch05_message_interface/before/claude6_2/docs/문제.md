문제 1-2: 보고서 생성기
```
public class ReportGenerator {
    public String generateSalesReport(Date startDate, Date endDate) {
        List<Order> orders = orderRepository.findByDateRange(startDate, endDate);
        
        double totalSales = 0;
        for (Order order : orders) {
            totalSales += order.getAmount();
        }
        
        int totalOrders = orders.size();
        double averageOrderValue = totalSales / totalOrders;
        
        return String.format("총 매출: %.2f원\n주문 건수: %d건\n평균 주문액: %.2f원", 
                           totalSales, totalOrders, averageOrderValue);
    }
}
```

정답 풀이
```
<details>
<summary>💡 정답 및 해설</summary>
평가:

명령-쿼리 분리: ✅ 통과

이 메서드는 순수한 쿼리 (상태 변경 없음, 값만 반환)
부수효과가 없어서 안전하게 호출 가능


의도를 드러내는 네이밍: ⚠️ 개선 필요

generateSalesReport는 "어떻게" 할지를 드러냄 (generate라는 구현 방법)
더 나은 이름: getSalesReport, calculateSalesReport



하지만 진짜 문제는 다른 곳에:

java// 문제: Order가 단순 데이터 객체처럼 사용됨
for (Order order : orders) {
    totalSales += order.getAmount();  // Order에게 묻기만 함
}

// 개선: Order 컬렉션에게 스스로 계산하도록
totalSales = Order.calculateTotalAmount(orders);
// 또는
totalSales = orders.stream()
                   .mapToDouble(Order::getAmount)
                   .sum();

핵심 인사이트:

이 코드는 "기술적으로는 맞지만" Order를 자료구조처럼 취급
실무에서는 허용되지만, 객체지향 관점에서는 개선 여지 있음
트레이드오프: 간단한 집계는 이렇게 해도 OK, 복잡한 비즈니스 로직은 객체에 위임
```