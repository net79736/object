1단계: 개념 파악 (O/X 퀴즈)
	1	(O) 협력 관계에서 요청을 보내는 객체를 서버, 요청을 받는 객체를 클라이언트라고 한다.
	2	(O) '디미터 법칙'은 객체 내부의 구조를 묻지 말고, 원하는 작업을 시키라는 원칙이다.
	3	(O) screening.getMovie().getFee()와 같이 연쇄적으로 메시지를 전송하는 것은 전형적인 디미터 법칙 위반 사례다.
	4	(X) '명령-쿼리 분리 원칙'에 따르면, 객체의 상태를 변경하는 메서드(명령)는 반드시 변경된 상태 값을 반환해야 한다.
	5	(X) 객체가 '무엇을 하는지'보다 '어떻게 하는지'를 인터페이스 이름에 드러내는 것이 좋은 설계다.
	6	(O) 자료구조(Data Structure)인 경우에는 내부를 노출해야 하므로 디미터 법칙을 적용할 필요가 없다.

2단계: 상황 판단 및 용어 매칭
정리하신 텍스트 속의 세부 개념들을 구분하는 문제입니다.
	7	다음 중 '디미터 법칙'을 준수하기 위해 메시지를 전송할 수 있는 대상이 아닌 것은?
	◦	① this 객체
	◦	② 메서드의 인자로 전달된 객체
	◦	③ this의 속성(멤버 변수) 객체
	◦	④ 메서드 내에서 생성된 지역 객체
	◦	⑤ 메서드 인자로 전달된 객체가 가진 내부 멤버 객체 (정답)
	8	다음 설명에 해당하는 용어를 보기에서 고르세요."동일한 오퍼레이션 호출에 대해 서로 다른 메서드들이 실행되는 것"
	◦	보기: [  퍼블릭 인터페이스 ]

3단계: 실전 코드 리팩토링 (주관식)
가장 중요한 부분입니다. 코드를 보고 "설계의 냄새"를 맡아보세요.
	9	[의도를 드러내는 이름] 다음 두 메서드 이름을 클라이언트 관점에서 '무엇을 하는지'가 드러나도록 하나의 이름으로 통합한다면 무엇이 적당할까요?
	◦	isSatisfiedByPeriod(screening)
	◦	isSatisfiedBySequence(screening)
	◦	답변: isSatisfiedBy
	10	[기차 충돌 고치기] 아래 코드는 Theater가 Audience의 가방 속을 뒤지고 있습니다. 디미터 법칙을 지키도록 한 줄의 코드로 수정해 보세요.
	◦	기존: if (audience.getBag().hasInvitation()) { ... }
	◦	수정: 
```java
public class TicketSellter {
    private TicketOffice ticketOffice;

    public void sellTo(Audience audience) {
        ticketOffice.plusAmount(
            audience.buy(ticketOffice.getTicket())
        );
    }
}

public class Audience {
    private Bag bag;

    public Bag getBag() {
        return bag;
    }

    public Long buy(Ticket ticket) {
        return bag.hold(ticket);
    }
}

public class Bag {
    private Invitation invitation;
    private Money amount;
    private Ticket ticket;

    public Bag(Invitation invitation, Money amount, Ticket ticket) {
        this.invitation = invitation;
        this.amount = amount;
        this.ticket = ticket;
    }

    public void hold(Ticket ticket) {
        if (hasInvatation()) {
            this.ticket = ticket;
            return 0L;
        } else {
            this.ticket = ticket;
            minusAmount(ticket.getFee());
            return ticket.getFee();
        }
    }
    
    public boolean hasInvatation() {
        return invitation != null;
    }

    public void minusAmount(Long fee) {
        amount.minus(Money.wons(fee));
    }
}
```

	9	[캡슐화와 응집도] PeriodCondition 이 Screening 의 시작 시간을 가져와서 직접 할인 여부를 계산하는 로직이 있습니다. 이때 로직을 무조건 Screening으로 옮기는 것이 정답이 아닌 이유는 무엇인가요? (정리글 6.5.6 참고)
	◦	답변: Screening 은 이미 영화를 예매하는 책임을 떠맡고 있는데 추가로 할인 여부를 판단하는 로직을 추가하는 것이 SRP 에 위반된다고 판다하기 때문입니다.

4단계: 명령-쿼리 분리 (CQS)
	12	다음 메서드들을 [명령]과 [쿼리]로 분류하세요.
	◦	A. public void setTicket(Ticket ticket) [명령]
	◦	B. public Long getAmount() [쿼리]
	◦	C. public boolean isSatisfiedBy(Screening screening) [쿼리]
	◦	D. public void minusAmount(Long amount) [명령]






